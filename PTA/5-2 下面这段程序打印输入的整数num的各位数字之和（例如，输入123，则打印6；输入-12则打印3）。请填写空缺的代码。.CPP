#include <stdio.h>
int main(){
    int num;
    scanf("%d", &num);
    int sum;
    sum = 0;
    if( num < 0)  
;
    while( 
 ){
        sum +=  
;
        num =  
;
    }
    printf("%d\n", sum);
    return 0;
}