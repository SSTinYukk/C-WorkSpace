#include<stdio.h>
#include<string.h>
int week(char *s[])
int main()
{
    int n;
    char s[100][100];
    scanf("%d",&n);
    int i;
    for(i=0;i<n;i++){
        scanf("%s",&s[i]);
    }
    for(i=0;i<n;i++){

    }
}